{
  "name": "pipecat-ws",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn pipecat_ws.main:app --reload --port 8000",
    "build": "uv sync",
    "start": "uv run uvicorn pipecat_ws.main:app --host 0.0.0.0 --port 8000",
    "lint": "uv run ruff check .",
    "check-types": "echo \"pipecat-ws is a Python package; no TS type-check\""
  }
}
